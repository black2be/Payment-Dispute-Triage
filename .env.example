# Copy to .env on the EC2 host (do NOT commit .env).
# Domain that points (A record) at the EC2 public IP — enables auto-HTTPS via Caddy.
# Leave as localhost for local testing (Caddy uses an internal self-signed cert).
DOMAIN=localhost

# Seed mock data on first start (true once, then set back to false)
SEED=false
